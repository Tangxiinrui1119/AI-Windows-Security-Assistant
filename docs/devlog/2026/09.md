# 2026 年 9 月

> [📔 日志说明](README.md) ｜ [🏠 项目主页](https://tangxiinrui1119.github.io/AI-Windows-Security-Assistant/)

## 2026-09-06（Day 1）

### 今日目标
开始三年技术成长计划 Day 1：把三年规划落到仓库，第一次用 PowerShell 探索 Windows 启动项。

### 今日学习
- 建立公开仓库 AI-Windows-Security-Assistant，把三年规划整理入库（docs/PLAN.md）
- 了解 GitHub Pages：仓库可以直接变成网站，上线了项目主页（访客入口）
- 第一次使用 PowerShell 查询 Windows 启动项
- 学习 `Get-CimInstance`
- 学习 PowerShell 管道 `|`
- 初步认识 Windows 注册表：`HKLM` / `HKU` / `HKCU`
- 认识 `Run` 启动项
- 学习 SID，理解 Windows 用户身份和 SID 的关系

### 今日实践
```powershell
Get-CimInstance Win32_StartupCommand | Select-Object Name, Command, Location, User | Format-Table -AutoSize
Get-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run'
```
（管理员 PowerShell 中实际执行）
- 查到 16 条启动项；Location 列直接暴露了来源：`HKU\S-1-5-21-...-1001`（我自己的用户 SID！）、`S-1-5-19` / `S-1-5-20`（LocalService / NetworkService）、`S-1-5-80`（服务账户）、`HKLM`
- `Get-ItemProperty` 看 HKCU Run：实际只有 2 个值（OneDrive、SoftwareBoxAutoStart）
- 仓库与主页的搭建过程见提交历史

### 今日疑问
- 为什么一台电脑上会有多个 SID？
- 用户名和 SID 到底是什么关系？
- HKCU 和 HKU 到底是什么关系？
- PowerShell 为什么可以直接访问 Windows 注册表？
- `Run` 为什么能够影响程序开机/登录后的自动启动？
- `Get-CimInstance` 到底是什么？
- PowerShell 的 `|` 到底是什么？
- Windows 的用户、SID、注册表和权限之间有什么关系？
- （实验后新增）为什么 OneDriveSetup 会写进 S-1-5-19 / S-1-5-20 这些服务账户的 Run 里？
- （实验后新增）Win32_StartupCommand 显示我的 SID 的 Run 键有 10+ 条，但直接查 HKCU Run 只有 2 条——同一个位置为什么对不上？（待查证）

大部分疑问在今天的笔记里逐条整理了：[notes/powershell/01-windows-registry-startup-sid.md](../../notes/powershell/01-windows-registry-startup-sid.md)

### 今日状态
- 已完成：注册表根键 HKLM/HKU/HKCU 的概念与区别、Run 的作用、Get-CimInstance 与管道的基本用法、SID 的意义与结构；并且亲手在自己的输出里看到了 SID（S-1-5-21-...-1001）
- 还没完全懂：SID → 访问令牌 → ACL 的完整权限链路（概念理顺了，还没亲手观察过 ACL）；以及实验后新增的两个疑问（见上）

### 今日总结
今天真正建立起来的认识：PowerShell 能直接「看到」Windows 的注册表；启动项只是注册表里的一组键值；而注册表背后是「用户 = SID」的身份体系——用户名是给人看的，SID 才是 Windows 真正用来识别身份的。今天最爽的一刻，是亲眼在自己的输出里看到了自己的 SID（S-1-5-21-...-1001）：从「查启动项」一个动作，顺藤摸瓜摸到了 Windows 身份与安全模型的大门。

## 2026-09-07（Day 2）

### 今日目标
把昨天的 SID 往下挖一层：搞懂 Windows 权限判断的完整链路（SID → 组 → Access Token → ACL/ACE → 有效权限）。

### 今日学习
- 学习 NTFS：Windows 的文件系统，权限能力建立在它之上（NTFS ≠ ACL）
- 学习 ACL（访问控制列表）与 ACE（访问控制条目）：集合与单条规则的关系
- 学习 Allow / Deny：为什么 Deny 常常「压过」Allow，以及 Deny 只针对具体权限、不是一票否决
- 学习用户组与组 SID：权限可以授予组，组也是安全主体
- 学习 Access Token：登录后生成的「权限身份证」，包含用户 SID + 组 SID
- 学习权限继承：父对象 → 子对象；Explicit ACE 与 Inherited ACE；关闭继承 ≠ 立即删除现有权限
- 学习 Effective Permissions：最终有效权限，以及判断五步法
- 完成综合实战例题推演（Project → Secret → password.txt：Read ✅ / Write ❌）

### 今日实践
今天以理论 + 纸上推演为主，没有在真机上跑命令：
- 用「五步法」完整推演了权限例题，并分析了每条权限的来源（哪个 ACE 从哪个目录继承而来）

### 今日疑问
- （今天的学习材料里没有留下卡点）
- 留一个待验证项：纸上推演的结论，和真机上 `icacls` 看到的实际 ACL 是否一致？

### 今日状态
- 已完成：SID → 组 → Access Token → ACL/ACE → 有效权限的完整链路，能按五步法推演权限题
- 还没完全懂：真实系统里的边界情况（继承来的 Deny vs 显式 Allow、管理员令牌/UAC 的影响）——留到动手实验再补

### 今日总结
今天真正建立起来的认识：昨天学会了「SID 是什么」，今天学会了「SID 怎么决定权限」——用户名 → SID → 组 → Access Token → 匹配 ACL 里的 ACE → Allow/Deny + 继承 → 有效权限。最重要的不是背「Deny 优先」，而是学会按这条链一步一步推演。笔记：[notes/security/01-windows-permissions-basics.md](../../notes/security/01-windows-permissions-basics.md)
