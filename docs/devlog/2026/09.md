# 2026 年 9 月

> [📔 日志说明](README.md) ｜ [🏠 项目主页](https://tangxiinrui1119.github.io/AI-Windows-Security-Assistant/)

## 2026-09-06（Day 1）

### 今日目标
开始三年技术成长计划 Day 1：把三年规划落到仓库，第一次用 PowerShell 探索 Windows 启动项。

### 今日学习
- 建立公开仓库 AI-Windows-Security-Assistant，把三年规划整理入库（docs/PLAN.md）
- 了解 GitHub Pages：仓库可以直接变成网站，上线了项目主页（访客入口）
- 第一次使用 PowerShell 查询 Windows 启动项
- 学习 `Get-CimInstance`
- 学习 PowerShell 管道 `|`
- 初步认识 Windows 注册表：`HKLM` / `HKU` / `HKCU`
- 认识 `Run` 启动项
- 学习 SID，理解 Windows 用户身份和 SID 的关系

### 今日实践
```powershell
Get-CimInstance Win32_StartupCommand | Select-Object Name, Command, Location, User | Format-Table -AutoSize
Get-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run'
```
（管理员 PowerShell 中实际执行）
- 查到 16 条启动项；Location 列直接暴露了来源：`HKU\S-1-5-21-...-1001`（我自己的用户 SID！）、`S-1-5-19` / `S-1-5-20`（LocalService / NetworkService）、`S-1-5-80`（服务账户）、`HKLM`
- `Get-ItemProperty` 看 HKCU Run：实际只有 2 个值（OneDrive、SoftwareBoxAutoStart）
- 仓库与主页的搭建过程见提交历史

### 今日疑问
- 为什么一台电脑上会有多个 SID？
- 用户名和 SID 到底是什么关系？
- HKCU 和 HKU 到底是什么关系？
- PowerShell 为什么可以直接访问 Windows 注册表？
- `Run` 为什么能够影响程序开机/登录后的自动启动？
- `Get-CimInstance` 到底是什么？
- PowerShell 的 `|` 到底是什么？
- Windows 的用户、SID、注册表和权限之间有什么关系？
- （实验后新增）为什么 OneDriveSetup 会写进 S-1-5-19 / S-1-5-20 这些服务账户的 Run 里？
- （实验后新增）Win32_StartupCommand 显示我的 SID 的 Run 键有 10+ 条，但直接查 HKCU Run 只有 2 条——同一个位置为什么对不上？（待查证）

大部分疑问在今天的笔记里逐条整理了：[notes/powershell/01-windows-registry-startup-sid.md](../../notes/powershell/01-windows-registry-startup-sid.md)

### 今日状态
- 已完成：注册表根键 HKLM/HKU/HKCU 的概念与区别、Run 的作用、Get-CimInstance 与管道的基本用法、SID 的意义与结构；并且亲手在自己的输出里看到了 SID（S-1-5-21-...-1001）
- 还没完全懂：SID → 访问令牌 → ACL 的完整权限链路（概念理顺了，还没亲手观察过 ACL）；以及实验后新增的两个疑问（见上）

### 今日总结
今天真正建立起来的认识：PowerShell 能直接「看到」Windows 的注册表；启动项只是注册表里的一组键值；而注册表背后是「用户 = SID」的身份体系——用户名是给人看的，SID 才是 Windows 真正用来识别身份的。今天最爽的一刻，是亲眼在自己的输出里看到了自己的 SID（S-1-5-21-...-1001）：从「查启动项」一个动作，顺藤摸瓜摸到了 Windows 身份与安全模型的大门。

## 2026-09-07（Day 2）

### 今日目标
把昨天的 SID 往下挖一层：搞懂 Windows 权限判断的完整链路（SID → 组 → Access Token → ACL/ACE → 有效权限）。

### 今日学习
- 学习 NTFS：Windows 的文件系统，权限能力建立在它之上（NTFS ≠ ACL）
- 学习 ACL（访问控制列表）与 ACE（访问控制条目）：集合与单条规则的关系
- 学习 Allow / Deny：为什么 Deny 常常「压过」Allow，以及 Deny 只针对具体权限、不是一票否决
- 学习用户组与组 SID：权限可以授予组，组也是安全主体
- 学习 Access Token：登录后生成的「权限身份证」，包含用户 SID + 组 SID
- 学习权限继承：父对象 → 子对象；Explicit ACE 与 Inherited ACE；关闭继承 ≠ 立即删除现有权限
- 学习 Effective Permissions：最终有效权限，以及判断五步法
- 完成综合实战例题推演（Project → Secret → password.txt：Read ✅ / Write ❌）

### 今日实践
今天以理论 + 纸上推演为主，没有在真机上跑命令：
- 用「五步法」完整推演了权限例题，并分析了每条权限的来源（哪个 ACE 从哪个目录继承而来）

### 今日疑问
- （今天的学习材料里没有留下卡点）
- 留一个待验证项：纸上推演的结论，和真机上 `icacls` 看到的实际 ACL 是否一致？

### 今日状态
- 已完成：SID → 组 → Access Token → ACL/ACE → 有效权限的完整链路，能按五步法推演权限题
- 还没完全懂：真实系统里的边界情况（继承来的 Deny vs 显式 Allow、管理员令牌/UAC 的影响）——留到动手实验再补

### 今日总结
今天真正建立起来的认识：昨天学会了「SID 是什么」，今天学会了「SID 怎么决定权限」——用户名 → SID → 组 → Access Token → 匹配 ACL 里的 ACE → Allow/Deny + 继承 → 有效权限。最重要的不是背「Deny 优先」，而是学会按这条链一步一步推演。笔记：[notes/security/01-windows-permissions-basics.md](../../notes/security/01-windows-permissions-basics.md)

## 2026-09-08（Day 3）

### 今日目标
把前两天学的权限链路（SID → 用户/组 → Access Token → ACL → ACE → Allow/Deny → 有效权限）放到 Windows 实机上，用 `icacls` 逐项验证。

### 今日学习
- 用 `icacls` 查看真实 ACL，理解 `(I)(OI)(CI)(F)` 等标记
- 对比 `C:\Users\73360` 与 Desktop 的 ACL，区分显式 ACE 与继承 ACE
- 新建测试文件，实际验证目录 → 文件的权限继承
- 用 `/grant` 实际添加 Allow ACE、`/deny` 添加 Deny ACE
- 理解多个 ACE 同时作用：多个 Allow 共同贡献权限，判断要落到具体请求的权限上
- 纠正「Deny 永远优先」的死记法：按判断链一步一步推演
- 完成综合练习（secret.txt：Read ✅ / Write ✅ / Delete ❌）

### 今日实践
```powershell
icacls "$env:USERPROFILE\Desktop"          # 观察继承 ACE：(I)(OI)(CI)(F)
icacls "$env:USERPROFILE"                  # 对比：显式 ACE，没有 (I)
New-Item "$env:USERPROFILE\Desktop\acl-test.txt" -ItemType File
icacls "$env:USERPROFILE\Desktop\acl-test.txt"   # 文件上只有 (I)(F)，验证继承真的发生
icacls "$env:USERPROFILE\Desktop\acl-test2.txt" /grant "Everyone:(R)"   # 添加显式 Allow
icacls "$env:USERPROFILE\Desktop\acl-test2.txt" /deny "Everyone:(W)"    # 添加 Deny
```

### 今日疑问
- `OI/CI` 只是继承传播设置，不能单凭它证明后代当前已拥有该 ACE——已在实验中解答：新建文件后直接查看子对象 ACL 才是真正的验证。

### 今日状态
- 已完成：真实 ACL 的查看与解读、显式/继承 ACE 的区分、Allow/Deny 的实战添加与综合判断、根据 ACL 推导有效权限
- 还没完全懂：显式与继承 ACE 同时存在的更复杂边界（如显式 Deny vs 继承 Allow 的组合）——留待后续遇到再研究

### 今日总结
今天真正建立起来的认识：理论权限模型终于在真机上对上了号——icacls 输出里的 (I)(OI)(CI)(F) 就是 Day 2 学的显式/继承、Allow/Deny 的真实面孔。最重要的收获：不要死记「Deny 永远优先」，而是照着「我是谁 → 令牌里有哪些 SID → 匹配哪些 ACE → 请求的是哪个具体权限 → 最终有效权限」一步一步推演。笔记：[notes/security/02-windows-acl-effective-permissions.md](../../notes/security/02-windows-acl-effective-permissions.md)

## 2026-09-09（Day 4）

### 今日目标
进入 Windows 身份认证体系：理解「认证（Authentication）→ 授权（Authorization）」的全过程，把前三天的 SID/Token/ACL 知识和「密码如何变成身份」接起来。

### 今日学习
- 学习 Authentication（你是谁）与 Authorization（你能做什么）的区别与衔接
- 学习 Hash 基础：单向、不可逆，Hash ≠ 加密；弱密码容易破解是因为密码空间小，不是算法弱
- 学习 SAM 数据库：保存本地账户、SID、密码 Hash（位置 C:\Windows\System32\config\SAM）
- 学习 LSASS（lsass.exe）：Windows 安全认证核心——登录验证、调用认证协议、创建 Access Token
- 学习 Windows 登录流程：Winlogon → LSASS → SAM → 验证 Hash → 创建 Token
- 学习 NTLM 认证协议：NTLM 协议 ≠ NTLM Hash；Challenge-Response 流程（不发送密码，只证明知道密码）
- 学习 Pass-the-Hash：不是破解 Hash，而是用 Hash 直接冒充身份认证
- 学习 Access Token 的组成：User SID + Group SID + Privileges
- 把 Token + ACL 的授权判断与前三天的知识连成一条线

### 今日实践
今天以理论为主，没有在真机上跑命令（知识点：认证 → 授权的链路梳理，详见笔记）。

### 今日疑问
- 「显式 Deny 优先级最高」和 Day 3 说的「不要死记 Deny 永远优先」怎么统一？（已在笔记中注明：先匹配、再落到具体请求的权限——例子里是 Alice 自己的 SID 匹配到了显式 Deny Read，才拒绝 Read）
- 留待后续：NTLM 与 Kerberos 的区别（今天没有展开）

### 今日状态
- 已完成：认证/授权的关系、Hash 与 SAM 的基本认识、NTLM Challenge-Response 流程、Pass-the-Hash 的本质、Token 的组成
- 还没完全懂：LSASS 的内部具体机制、NTLM 更底层的细节——属于进阶，后续实战再补

### 今日总结
今天真正建立起来的认识：前三天的 SID/Token/ACL 是「授权」半边，今天补上了「认证」半边——密码 → Hash → SAM/LSASS → NTLM → Access Token。两条线在 Access Token 处合流：认证决定你是谁，授权决定你能做什么，Token 是两者的交接点。整条 Windows 安全链第一次完整地串起来了。笔记：[notes/security/03-windows-authentication-ntlm.md](../../notes/security/03-windows-authentication-ntlm.md)

## 2026-09-10（Day 5）

### 今日目标
进入 Windows 域安全：学习 Kerberos 认证体系（KDC / AS / TGS / TGT / SPN），理解它与昨天 NTLM 的区别，并了解 AD / DC 和 Kerberoasting 的基本原理。

### 今日学习
- 学习 Kerberos 与 NTLM 的区别：NTLM 是客户端-服务器的 Challenge/Response；Kerberos 依赖可信中心 KDC 签发 Ticket
- 学习 KDC 的两个角色：AS（初始认证、发 TGT）与 TGS（凭 TGT+SPN 发 Service Ticket）
- 学习 TGT 与 Service Ticket 的区别：TGT 证明「已被域认证」，Service Ticket 针对具体服务
- 学习 SPN：具体服务的唯一标识（如 cifs/FileServer01）
- 学习「Ticket ≠ 权限」：Kerberos 解决认证，SID/Token/ACL 解决授权
- 学习 AD（域环境中的目录与身份管理体系）与 DC（域控制器，KDC 通常运行其上）
- 学习 Kerberoasting 原理：获取 Service Ticket → 离线猜测服务账户密码
- 学习 Online / Offline 猜测的真正区别：验证猜测的位置（在服务器还是本地）
- 学习 Pass-the-Hash、Offline Hash Cracking、Kerberoasting 三者的区别

### 今日实践
今天以理论为主，没有在真机上跑命令。

### 今日疑问
- 无卡点。笔记最后留了 10 道复习题，明天开始学习前自测。

### 今日状态
- 已完成：Kerberos 认证流程（AS→TGT，TGT+SPN→TGS→Service Ticket）、Ticket≠权限、AD/DC/KDC 的关系、Kerberoasting 与 PtH 的区别
- 还没完全懂：Kerberos 的加解密细节（密钥派生、Ticket 内部结构）——属于进阶，后续需要时再深入

### 今日总结
今天真正建立起来的认识：NTLM 是客户端-服务器之间的点对点认证，Kerberos 是引入可信第三方 KDC 的票据体系；但无论哪种认证，最后都只解决「你是谁」——「你能做什么」仍然回到 SID/Token/ACL。域安全的骨架第一次搭起来了：AD 管目录、DC 跑服务、KDC 发票据、ACL 定权限。笔记：[notes/security/04-windows-kerberos-ad.md](../../notes/security/04-windows-kerberos-ad.md)

## 2026-09-14（Day 6）

### 今日目标
在 Day 5 的 Kerberos 基础流程上，深入理解 Ticket 与 Session Key：为什么 Ticket 改不了、两者各自解决什么问题，并复习三类凭据攻击（PTH / PTT / Kerberoasting）。

### 今日学习
- 学习 Kerberos Ticket 为什么不能被客户端随意修改：Ticket 由 KDC 用目标服务密钥保护，客户端没有该密钥，无法解密 / 修改 / 重造
- 学习加密的两个作用：Confidentiality（机密性）与 Integrity（完整性）——被篡改的 Ticket 过不了服务器的完整性验证
- 学习 Service Ticket 的作用：解决「服务器为什么相信你是谁」（认证）
- 学习 Session Key（会话密钥）：认证完成后保护客户端与服务器之间的后续通信
- 学习 Ticket 与 Session Key 的区别：身份证明 vs 通信钥匙
- 复习三类凭据攻击：PTH（NTLM Hash）、PTT（Kerberos Ticket）、Kerberoasting（Service Ticket 材料 → 离线破解）

### 今日实践
今天以理论为主，没有在真机上跑命令。

### 今日疑问
- 无卡点。（笔记里的「下一步学习计划」：Day 7 学 AS-REQ/AS-REP、TGS-REQ/TGS-REP 完整流程）

### 今日状态
- 已完成：Ticket 不可篡改的原理、Ticket vs Session Key、PTH/PTT/Kerberoasting 三者的区别
- 还没完全懂：Kerberos 报文级流程（AS/TGS 四步交换）—— Day 7 的主题

### 今日总结
今天真正建立起来的认识：Kerberos 的 Ticket 之所以可信，靠的是「用目标服务密钥加密」这件事本身——机密性防止别人读，完整性防止别人改，而客户端因为没密钥，连「造一个假 Ticket」都做不到。另外三类凭据攻击终于分清了：Hash → PTH，Ticket → PTT，Service Ticket 材料 → Kerberoasting。笔记：[notes/security/05-windows-kerberos-ticket-session-key.md](../../notes/security/05-windows-kerberos-ticket-session-key.md)

## 2026-09-15（Day 7）

### 今日目标
学习 Windows 事件日志与安全审计，利用事件字段关联具体登录会话，把多个事件串成时间线，并区分客观事实与猜测。

### 今日学习
- 认识 Windows Event Log、Event Viewer 与 Security Log
- 学习事件编号：4624（登录成功）、4625（登录失败）、4634（登录会话结束）、4688（新进程创建）
- 学习 Logon Type：2（本地交互）、3（网络）、5（服务）、7（解锁）、10（远程交互）、11（缓存交互）
- 学习 Account Name、Source Network Address 与 Logon ID 的作用：账户身份、网络来源和具体登录会话
- 理解同一个账户也可以拥有多个 Logon ID，不能只凭用户名关联事件
- 学习 Audit：对应审计需要提前开启，事后开启不会自动补回以前没有记录的事件
- 学习 Timeline：先确认事实，再判断异常，继续寻找证据；PowerShell 或登录失败本身不能直接证明攻击

### 今日实践
通过 `eventvwr.msc` 打开事件查看器，进入「Windows 日志 → 安全」，筛选 `4624,4625,4634`。
- 实际查看 4624：SYSTEM / S-1-5-18 / Logon Type 5 / Logon ID 0x3E7 / services.exe / 来源地址为 `-`，初步判断与正常系统服务行为相符
- 实际查看 4634：账户 73360 / Logon Type 2 / Logon ID 0xFFF5D73，理解为某个本地交互式登录会话结束
- 尝试按 Logon ID 寻找对应的 4624，本次没有找到；未将会话关联练习当成已经完成的实机结果

### 今日疑问
- 为什么不是所有 4624 都代表有人输入密码登录？已理解：还要看 Logon Type，Type 5 可以是系统服务登录。
- 对应 Logon ID 的 4624 为什么暂时找不到？目前没有足够证据确定原因，留待以后继续查证。

### 今日状态
- 已完成：事件编号、登录类型、Logon ID、审计与时间线基础；事件查看器实机查看及 4624 / 4634 分析
- 待继续：实机串联同一会话的多个事件，以及进程创建审计和命令行记录的具体配置
- 下次先做 Day 7 短复习，再进入 Day 8 新知识

### 今日总结
今天开始把之前的身份、认证和权限知识连接到「行为发生后如何留下证据」：Event ID 告诉我发生了什么，Logon Type 告诉我怎么登录，Logon ID 帮助关联具体会话，Audit 决定哪些行为会被记录。分析时先确认日志里真正存在的事实，再判断是否可疑并继续取证。笔记：[06 · Windows 事件日志与安全审计（Day 7）](../../notes/security/06-windows-event-log-security-audit.md)

## 2026-09-16（C++ 基础）

### 今日目标
学习并巩固指针、引用与 const，能够通过代码推导变量的值、指针的指向和参数修改对原对象的影响。

### 今日学习
- 对比值传递与引用传递：副本与原变量别名的区别
- 学习普通指针，区分 x、&x、p、*p，以及修改 p 与修改 *p 的不同作用
- 学习二级指针 int **pp，理解 pp → p → x，以及通过 *pp 改变 p 的指向
- 学习指针的引用 int *&r，理解 r 是指针变量 p 的别名
- 对比 const int *p、int *const p、const int *const p 的限制
- 理解引用绑定后不能重新绑定，a = y 是赋值而不是换绑
- 学习常量引用与 const T& 函数参数：通过引用只读访问对象，避免复制已有的大对象
- 学习 const 引用绑定临时值，以及 show("Jerry") 中临时字符串对象的基本概念

### 今日实践
围绕笔记中的代码进行结果推导：
- 普通指针与二级指针示例：修改所指数据、切换指向后，得到 x = 30、y = 25
- 指针引用示例：r = &y 改变 p 的指向，*r = 50 后得到 x = 10、y = 50
- 对比三种 const 指针下哪些赋值允许、哪些不允许
- 对比 T、T&、const T& 作为函数参数的行为

### 今日疑问
- const int *p 是否会把原来的 x 变成常量？已理解：它限制通过 p 修改对象。
- int &a = x 后执行 a = y 是否会重新绑定？已理解：它将 y 的值赋给 x，a 仍然引用 x。
- 数组名为什么能赋给指针、p + 1 表示什么？留到下一次正式学习。

### 今日状态
- 已完成：值与引用传递、普通与二级指针、指针引用、const 指针、常量引用和临时对象基础
- 已能通过代码自行推导这些声明与参数形式的含义
- 仅接触、未深入：右值引用与移动语义
- 尚未正式学习：vector / STL / 迭代器，以及数组与指针的关系
- 下次从 int a[3] = {10, 20, 30}; int *p = a; 开始，学习数组与指针

### 今日总结
今天最重要的认识是区分「指针保存的地址」与「地址对应的数据」，以及「对象本身」与「通过某个指针或引用访问对象」：p = &y 改指向，*p = 50 改数据；引用是别名，绑定后不能换对象；const 指针和常量引用限制的是相应的修改方式。笔记：[01 · C++ 指针、引用与 const](../../../notes/cpp/01-pointers-references-const.md)

## 2026-09-16（Day 8）

### 今日目标
理解 Windows 进程、PID、PPID、父子进程和进程树，并把进程行为与登录会话关联起来。

### 今日学习
- 复习 Logon ID、4634 与 4688，明确不能只凭进程名判断异常
- 学习进程、PID 与 PPID：进程名说明运行什么，PID 标识具体实例，PPID 指向创建它的父进程
- 学习通过“子进程 PPID = 父进程 PID”寻找父子关系，并将多层关系组成进程树
- 区分进程树与安全事件时间线：前者描述创建关系，后者还要结合 Logon ID、事件、时间与来源
- 理解 PID 会在进程结束后被重新使用，分析时需要结合时间、进程名、父进程和日志
- 理解父子进程关系不等于权限高低；PID/PPID 描述进程关系，Access Token 描述身份与权限

### 今日实践
在 PowerShell 中执行：

```powershell
Get-CimInstance Win32_Process |
Select-Object Name, ProcessId, ParentProcessId |
Sort-Object ProcessId |
Format-Table -AutoSize
```

- 实际观察到 `explorer.exe (PID 9368) → powershell.exe (PID 8948)`
- 实际观察到一条进程树分支：`explorer.exe → ChatGPT.exe → codex.exe → cmd.exe`

### 今日状态
- 已完成：Process、PID、PPID、Process Tree，以及和 Logon ID / 4688 的基础关联
- 已能使用 PowerShell 查看本机 PID / PPID，并根据它们推导父子进程关系
- 今天未深入：进程注入、DLL、恶意持久化与更复杂的进程攻击技术
- 下次先短复习，再沿当前 Windows 安全路线学习新内容

### 今日总结
今天把“谁登录、哪次会话”进一步连到“哪个进程、谁创建了它”。Logon ID 用来关联登录会话，PID 用来识别具体进程，PPID 用来寻找父进程；把这些信息和时间、4688 日志一起看，才能逐步还原行为关系。笔记：[07 · Windows 进程、PID、PPID 与进程树（Day 8）](../../notes/security/07-windows-processes-pid-ppid-process-tree.md)

## 2026-09-17（C++ 基础）

### 今日目标
理解数组名、首元素地址和指针运算的关系，并掌握数组作为函数参数时的行为。

### 今日学习
- 数组本身与指针的区别；数组名在很多表达式中会转换为首元素地址
- 指针运算：`arr + i` 与 `*(arr + i)` 的含义，以及 `arr[i] == *(arr + i)`
- 数组传参时 `int arr[]` 与 `int* arr` 的等价关系、为什么要额外传入 `size`
- `sizeof` 在完整数组与函数参数中的不同结果
- 通过函数读写原数组，以及 `i[arr]` 这一仅用于理解的小知识点

### 今日实践
完成数组遍历与元素修改练习，并用综合题推导偶数下标元素加 10 后的结果：`1 2 3 4 5 → 11 2 13 4 15`。

### 今日状态
- 已完成：数组与首元素地址、指针加法、下标访问、数组传参、长度传递和原数组修改
- 下次先短复习，再进入新的 C++ 基础内容

### 今日总结
数组不是指针，但数组名在很多表达式中会变成首元素地址，因此可以用指针运算访问元素：`arr[i] == *(arr + i)`。数组传入函数后参数实际是指针，函数不能仅靠它得知长度，通常需要同时传 `size`。笔记：[02 · C++ 数组与指针的关系](../../../notes/cpp/02-arrays-and-pointers.md)

## 2026-09-17（Day 9）

### 今日目标
在 PID、PPID 和进程树的基础上，学习通过程序路径、完整命令行和运行身份调查 Windows 进程。

### 今日学习
- 理解只看进程名不足以判断进程真实用途，还要结合 ExecutablePath、CommandLine、Owner 和父进程关系
- 区分 ExecutablePath（程序文件在哪里）与 CommandLine（启动程序时让它做什么）
- 使用 PID 与 PPID 确认父子进程关系，并理解父进程和运行身份是两件不同的事
- 学习 Owner：进程当前以哪个账户身份运行；用户名不是 SID，SID 才是 Windows 的重要安全身份标识
- 把 Owner 连接到 Access Token、ACL 和权限判断

### 今日实践
使用 `Get-CimInstance Win32_Process` 查看 Name、ProcessId、ParentProcessId、ExecutablePath 和 CommandLine。
- 观察到 VS Code 集成终端中的 `powershell.exe`：程序位于系统 PowerShell 路径，命令行包含 `-noexit`、`-command` 和 `shellIntegration.ps1`
- 根据 `python.exe` 的 PPID 等于 PowerShell 的 PID，确认它是该 PowerShell 的子进程
- 使用 `Invoke-CimMethod -MethodName GetOwner` 查询到该 PowerShell 以本机账户 `难办叔叔的电脑\\73360` 身份运行，`ReturnValue = 0` 表示查询成功

### 今日状态
- 已完成：ExecutablePath、CommandLine、Owner 的含义，以及它们和 PID / PPID / 进程树的关联
- 已能区分程序文件位置、启动参数、创建关系和运行身份
- 下次先短复习，再继续沿 Windows 进程调查路线学习

### 今日总结
今天把进程树向下延伸成一套更完整的调查方法：先看进程叫什么、编号是什么、谁创建了它，再看程序文件实际在哪里、启动时执行了什么，以及它以谁的身份运行。Owner 再连接到 SID、Access Token 与 ACL，最终回到 Windows 的权限判断。笔记：[08 · Windows 进程调查：路径、命令行与运行身份（Day 9）](../../notes/security/08-windows-process-investigation-path-commandline-owner.md)
