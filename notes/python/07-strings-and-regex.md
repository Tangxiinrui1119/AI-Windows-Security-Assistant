# 第7章 字符串基本操作与正则表达式

> 来源：课程 PPT《第7章 字符串基本操作与正则表达式》
> 复习目标：掌握字符串常用方法、格式化输出、正则表达式与 re 库，并了解异常处理。

## 一、知识地图

    字符串处理与正则表达式
    ↓
    ⭐ 常用字符串方法
    ├─ split() → 拆分，返回列表
    ├─ join() → 连接序列
    ├─ strip() → 去除首尾字符
    ├─ count() → 统计次数
    ├─ startswith() → 判断开头
    ├─ endswith() → 判断结尾
    └─ replace() → 替换
    ↓
    ⭐ 字符串格式化
    ├─ % 格式化
    ├─ str.format()
    │  └─ 填充 / 对齐 / 宽度 / 精度 / 类型
    └─ f-string
       ├─ f"...{变量}..."
       └─ 如 {x:.2f}
    ↓
    转义字符
    ├─ \n / \t / \\
    └─ r"..." 原始字符串
    ↓
    ⭐ 正则表达式 Regular Expression
    ├─ 用规则描述字符串模式
    ├─ 普通字符 → 精确匹配
    └─ 元字符 → 描述匹配规则
    ↓
    正则元字符
    ├─ . / | / []
    ├─ * / + / ?
    ├─ {n} / {m,n} / {n,}
    ├─ ^ / $
    └─ () 分组
    ↓
    ⭐ 特殊序列
    ├─ \d / \D
    ├─ \w / \W
    └─ \s / \S
    ↓
    ⭐ re 模块
    ├─ compile() → 生成正则对象
    ├─ search() → 整体扫描，找第一个匹配
    ├─ findall() → 所有匹配，返回列表
    ├─ finditer() → 所有匹配，返回迭代器
    ├─ match() → 只从开头匹配
    ├─ fullmatch() → 整个字符串完整匹配
    ├─ split() → 按正则拆分
    ├─ sub() → 替换，返回字符串
    └─ subn() → 替换结果 + 替换次数
    ↓
    网页数据解析示例
    └─ 正则提取 → 列表 / 字典保存 → 输出
    ↓
    异常处理
    ├─ try / except
    ├─ 多个 except
    ├─ else
    └─ finally

> [!IMPORTANT]
> 本章重点：
> **⭐ split / join / strip / replace 等字符串方法**
> **⭐ f-string 格式化**
> **⭐ \d、+、*、{n}、^、$ 等基础正则规则**
> **⭐ search / findall / match / fullmatch 的区别**
> **⭐ try-except 的异常处理结构**
## 二、字符串常用方法

### split()

按指定分隔符拆分字符串，返回列表。

    "a,b,c".split(",")
    # ["a", "b", "c"]

### join()

用指定分隔符连接序列中的字符串。

    ",".join(["a", "b", "c"])
    # "a,b,c"

### strip()

移除字符串首尾指定字符；未指定时默认处理空格、换行、制表等。

### count()

统计子字符串出现次数。

### startswith() / endswith()

判断字符串开头 / 结尾是否符合指定内容，返回 True / False。

### replace()

替换字符串内容。

## 三、字符串格式化

PPT 介绍三类：

### %

旧式格式化。

### str.format()

    "{} {}".format(a, b)

槽位可控制参数序号、填充、对齐、宽度、千位分隔、精度和类型。

### f-string

Python 3.6 之后加入：

    name = "Lily"
    age = 18
    f"{name} is {age} years old"

格式化：

    f"{x:.2f}"

表示保留 2 位小数。

## 四、转义字符

常见：

    
   换行
    	   制表
    \   反斜杠
    "   双引号
    '   单引号

不希望转义时可使用原始字符串：

    r"..."

## 五、正则表达式是什么

正则表达式用于描述“字符串应满足什么规则”，从而搜索或匹配字符串模式。

## 六、常用正则符号

    .        任意字符
    []       字符集合
    |        或
    *        前一规则 0 次或多次
    +        前一规则 1 次或多次
    ?        前一规则 0 次或 1 次
    {n}      正好 n 次
    {m,n}    m 到 n 次
    ^        字符串开头
    $        字符串结尾
    ()       分组

特殊序列：

    d   数字
    D   非数字
    w   字母、数字、下划线
    W   与 w 相反
    s   空白字符
    S   非空白字符

例如：

    d+

表示一个或多个连续数字。

## 七、re 模块

导入：

    import re

### search()

扫描整个字符串，找第一个匹配。

### findall()

找出所有匹配，返回列表。

### finditer()

作用类似 findall，但返回迭代器，适合较大数据。

### match()

只从字符串开头开始匹配。

### fullmatch()

要求从头到尾完整匹配。

### split()

按正则匹配规则拆分字符串。

### sub()

替换匹配到的内容，返回替换后的字符串。

### subn()

返回：

    (替换后的字符串, 替换次数)

### compile()

把模式字符串编译成正则表达式对象，便于重复使用。

## 八、匹配失败

匹配不上通常不是程序报错。

例如 match() 找不到时会返回：

    None

## 九、异常处理

PPT 后半部分介绍：

    try:
        可能发生异常的代码
    except 某异常类型:
        异常处理代码

可以有多个 except。

还可配合：

    else
    finally

其中 finally 中的代码无论是否发生异常都会执行。

## 十、本章最该记住

    split / join / strip / replace
    f-string
    d  +  *  {n}  ^  $
    search / findall / match / fullmatch
    try / except
